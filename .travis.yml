dist: xenial
language: python
python:
  - 3.6

addons:
  apt:
    packages:
    - libgirepository1.0-dev

install:
  - pip install tox

script:
  - tox -e flake8,pylint,check-py27,check-py36

after_success:
  - tox -e coveralls
