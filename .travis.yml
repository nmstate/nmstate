sudo: false
dist: trusty
language: python
python:
  - 3.6

install:
  - pip install tox
  - sudo apt-get install -qq -y python-gi python3-gi libgirepository1.0-dev

script:
  - tox --sitepackages -e flake8,pylint,check-py27,check-py36,coveralls
